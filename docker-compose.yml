version: '3.8'

services:
  ad-analytics:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ad-analytics-app
    restart: unless-stopped
    ports:
      - "80:80"       # Default HTTP port. Change to "3000:80" or "8080:80" if port 80 is occupied by a reverse proxy
    environment:
      - NODE_ENV=production
    healthcheck:
      test: ["CMD", "wget", "--quiet", "--tries=1", "--spider", "http://localhost:80/"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 5s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
